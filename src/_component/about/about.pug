.thumbnail
  img(src="asset/image/background-top.jpg")

.about
  .about-wrapper
    .about-portrait-wrapper
      img.about-portrait(src=resume.about.portrait.url)
    .about-me-wrapper
      h1.about-me-title #{resume.about.title}
      span.about-me-underline #{resume.about.underline}
  each contentItem in resume.about.content
    .about-me-content
      p #{contentItem}         
