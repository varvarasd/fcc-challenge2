#portfolio
.portfolio
  .portfolio-title #{resume.title}
  .portfolio-underline #{resume.about.underline}
  each content in resume.content
    .portfolio-subtitle #{content.title}
    a.portfolio-link(href=content.link, target="_blank")    
      img.portfolio-image(src=content.url)
